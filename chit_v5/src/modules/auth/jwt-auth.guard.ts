import {CanActivate,ExecutionContext,Injectable,UnauthorizedException} from '@nestjs/common'; import {ConfigService} from '@nestjs/config'; import {JwtService} from '@nestjs/jwt';
@Injectable() export class JwtAuthGuard implements CanActivate { constructor(private jwt:JwtService,private config:ConfigService){} async canActivate(c:ExecutionContext){const r=c.switchToHttp().getRequest();const h=r.headers.authorization as string|undefined;if(!h?.startsWith('Bearer '))throw new UnauthorizedException();try{r.user=await this.jwt.verifyAsync(h.slice(7),{secret:this.config.getOrThrow('JWT_ACCESS_SECRET')});return true;}catch{throw new UnauthorizedException();}}}
