import{User}from'@/src/types';export const hasRole=(u:User|null,r:string)=>!!u?.roles?.includes(r as any);export const isAdmin=(u:User|null)=>hasRole(u,'ADMIN');export const isAgent=(u:User|null)=>hasRole(u,'AGENT');export const isMember=(u:User|null)=>hasRole(u,'MEMBER');export const isCreator=(u:User|null,chit:any)=>!!u&&!!chit&&chit.creator_id===u.id;export const canOperate=(u:User|null,chit:any,permission?:string,access?:any)=>isAdmin(u)||isCreator(u,chit)||(isAgent(u)&&(!permission||access?.[permission]===true));
