import React,{useEffect,useState}from'react';import{ScrollView,Text,View}from'react-native';import{router,useLocalSearchParams}from'expo-router';import{chitsApi,reconciliationApi}from'@/src/api/all';import{Card,Loading,Screen,Stat,s}from'@/src/components/UI';import{money,errMsg}from'@/src/lib/format';
export default function Reconciliation(){const{chitId,monthId}=useLocalSearchParams<{chitId:string;monthId?:string}>();const[data,setData]=useState<any>();const[error,setError]=useState('');useEffect(()=>{(async()=>{try{const c=String(chitId);if(monthId){const r=await reconciliationApi.monthly(c,String(monthId));setData(r.data?.data??r.data)}else{const r=await reconciliationApi.summary(c);setData(r.data?.data??r.data)}}catch(e){setError(errMsg(e))}})()},[chitId,monthId]);if(!data&&!error)return <Screen title="Reconciliation" back={()=>router.back()}><Loading/></Screen>;if(error)return <Screen title="Reconciliation" back={()=>router.back()}><Card><Text style={s.danger}>{error}</Text></Card></Screen>;return <Screen title="Reconciliation" subtitle={monthId?'Monthly financial reconciliation':'Chit financial summary'} back={()=>router.back()}><ScrollView><Card><Text style={s.section}>{monthId?`Month ${data.month_number??'—'}`:'Financial summary'}</Text>{monthId?<><View style={s.row}><Stat label="Collected" value={money(data.collected)}/><Stat label="Payout settled" value={money(data.settled_payout)}/></View><View style={s.row}><Stat label="Outstanding" value={money(data.outstanding)}/><Stat label="Paid members" value={data.paid_members}/></View><Text style={s.muted}>Status: {String(data.status||'—')}</Text></>:<><Text>Collected: {money(data.financial?.collected)}</Text><Text>Paid out: {money(data.financial?.paid_out)}</Text><Text>Outstanding: {money(data.financial?.outstanding)}</Text></>}</Card></ScrollView></Screen>}
