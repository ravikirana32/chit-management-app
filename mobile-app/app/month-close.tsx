import React,{useState}from'react';import{Alert,Text}from'react-native';import{router,useLocalSearchParams}from'expo-router';import{closeApi}from'@/src/api/all';import{Button,Card,Screen,s}from'@/src/components/UI';import{errMsg}from'@/src/lib/format';
export default function MonthClose(){const{monthId}=useLocalSearchParams<{monthId:string}>();const[busy,setBusy]=useState(false);const close=async()=>{setBusy(true);try{const r=await closeApi.month(String(monthId));Alert.alert('Month locked','The month has been financially closed.',[{text:'OK',onPress:()=>router.back()}]);}catch(e){Alert.alert('Cannot close month',errMsg(e))}finally{setBusy(false)}};return <Screen title="Month Close" subtitle="Final reconciliation → LOCKED" back={()=>router.back()}><Card><Text style={s.section}>Lock this month</Text><Text style={s.muted}>Use this only after the draw/auction/agent payout is settled and all obligations are resolved. Locked months cannot be normally edited.</Text><Button title="Close & Lock Month" onPress={close} disabled={busy}/></Card></Screen>}
