import React,{useEffect,useState}from'react';import{Alert,ScrollView,Text}from'react-native';import{router,useLocalSearchParams}from'expo-router';import{chitsApi,drawsApi,agentApi}from'@/src/api/all';import{Button,Card,Loading,Screen,s}from'@/src/components/UI';import{errMsg,money}from'@/src/lib/format';import{useAuth}from'@/src/state/Auth';import{isAdmin,isCreator,isAgent}from'@/src/state/roles';export default function AgentMonth(){const{chitId,monthId}=useLocalSearchParams<{chitId:string;monthId:string}>();const{user}=useAuth();const[chit,setChit]=useState<any>();const[month,setMonth]=useState<any>();const[access,setAccess]=useState<any>();const[busy,setBusy]=useState(false);useEffect(()=>{(async()=>{try{const r=await chitsApi.get(String(chitId));const d=r.data?.data??r.data;setChit(d);setMonth(d.months?.find((m:any)=>m.id===monthId));if(isAgent(user)&&!isCreator(user,d))try{const a=await agentApi.chit(String(chitId));setAccess(a.data?.data??a.data)}catch{}}catch{}})()},[chitId,monthId,user]);if(!chit||!month)return <Screen title="Agent Month"><Loading/></Screen>;const allowed=isAdmin(user)||isCreator(user,chit)||!!(isAgent(user)&&access?.can_collect_cash);const settle=async()=>{setBusy(true);try{const r=await drawsApi.agentPayout(String(chitId),String(monthId));Alert.alert('Agent month settled',JSON.stringify(r.data?.data??r.data))}catch(e){Alert.alert('Unable to settle',errMsg(e))}finally{setBusy(false)}};return <Screen title="Agent Chit Month" subtitle="NO DRAW / NO BID" back={()=>router.back()}><ScrollView><Card><Text style={s.section}>Month {month.month_number}</Text><Text>All active members pay: {money(month.scheduled_amount)} each.</Text><Text style={s.success}>Configured agent payout: {money(month.winner_payout_amount)}</Text><Text style={s.muted}>Previous savings remain with the agent and roll forward to the next month.</Text></Card>{allowed&&<Button title="Settle Agent Month" onPress={settle} disabled={busy}/>}</ScrollView></Screen>}
