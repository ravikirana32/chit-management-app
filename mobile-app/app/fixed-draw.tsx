import React,{useEffect,useState}from'react';import{Alert,ScrollView,Text,View}from'react-native';import{router,useLocalSearchParams}from'expo-router';import{drawsApi,chitsApi,agentApi,operationsApi}from'@/src/api/all';import{Badge,Button,Card,Loading,Screen,s}from'@/src/components/UI';import{errMsg,money}from'@/src/lib/format';import{useAuth}from'@/src/state/Auth';import{isMember,isCreator,isAgent,canOperate}from'@/src/state/roles';
export default function FixedDraw(){const{chitId,monthId}=useLocalSearchParams<{chitId:string;monthId:string}>();const{user}=useAuth();const[state,setState]=useState<any>(null);const[chit,setChit]=useState<any>();const[access,setAccess]=useState<any>();const[policy,setPolicy]=useState<any>();const[loading,setLoading]=useState(true);const[busy,setBusy]=useState(false);const[error,setError]=useState('');
 const load=async()=>{setLoading(true);setError('');try{const c=await chitsApi.get(String(chitId));const cd=c.data?.data??c.data;setChit(cd);try{const q=await operationsApi.policy();setPolicy(q.data?.data??q.data)}catch{}try{const d=await drawsApi.get(String(chitId),String(monthId));setState(d.data?.data??d.data)}catch(e){setState(null);const msg=errMsg(e);if(msg&&msg!=='Draw not found')setError(msg)}if(isAgent(user)&&!isCreator(user,cd)){try{const a=await agentApi.chit(String(chitId));setAccess(a.data?.data??a.data)}catch{setAccess(undefined)}}}catch(e){setError(errMsg(e))}finally{setLoading(false)}};
 useEffect(()=>{if(chitId&&monthId)load()},[chitId,monthId,user?.id]);
 if(loading)return <Screen title="Fixed Draw" back={()=>router.back()}><Loading/></Screen>;if(error)return <Screen title="Fixed Draw" subtitle="Unable to load this draw" back={()=>router.back()}><Card><Text style={s.danger}>{error}</Text><Button title="Retry" onPress={load}/></Card></Screen>;if(!chit)return <Screen title="Fixed Draw" back={()=>router.back()}><Card><Text>Chit is unavailable.</Text></Card></Screen>;
 const month=chit.months?.find((m:any)=>String(m.id)===String(monthId));const operate=canOperate(user,chit,'can_run_draw',access);const bypass=Boolean(policy?.scheduleBypassEnabled);const scheduledDate=month?.scheduled_date;const today=new Intl.DateTimeFormat('en-CA',{timeZone:chit?.timezone||'Asia/Kolkata',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());const scheduleReady=bypass||!scheduledDate||String(scheduledDate).slice(0,10)===today;const status=String(state?.status||'NOT_STARTED').toUpperCase();const drawOpened=Boolean(state?.id||state?.drawId)&&status!=='COMPLETED';const interestOpen=drawOpened&&(!state?.draw_interest_opens_at||bypass||Date.now()>=new Date(state.draw_interest_opens_at).getTime())&&(!state?.draw_interest_closes_at||bypass||Date.now()<=new Date(state.draw_interest_closes_at).getTime());const payout=state?.winner_payout_amount??state?.winnerPayoutAmount??month?.winner_payout_amount;
 const start=async()=>{if(!scheduleReady)return Alert.alert('Draw not available yet',`Scheduled date: ${String(scheduledDate).slice(0,10)}.`);setBusy(true);try{await drawsApi.start(String(chitId),{chitMonthId:String(monthId)});await load();Alert.alert(bypass?'Test draw opened':'Draw opened','Members can now express interest.')}catch(e){Alert.alert('Open failed',errMsg(e))}finally{setBusy(false)}};
 const interest=async(v:boolean)=>{setBusy(true);try{await drawsApi.interest(String(chitId),String(monthId),v);await load()}catch(e){Alert.alert('Interest failed',errMsg(e))}finally{setBusy(false)}};
 const run=async()=>{setBusy(true);try{await drawsApi.run(String(chitId),String(monthId));await load();Alert.alert('Draw completed','The winner has been selected and the payout is pending settlement.')}catch(e){Alert.alert('Run failed',errMsg(e))}finally{setBusy(false)}};
 return <Screen title="Fixed Draw" subtitle="Open → interest → run → payout" back={()=>router.back()}><ScrollView>
  {bypass&&<Card><Text style={{fontWeight:'900'}}>⚠ TEST MODE</Text><Text style={s.muted}>Schedule date/time restrictions are bypassed by the API. Authorization, eligibility, locked-month and financial rules still apply.</Text></Card>}
  <Card><Text style={s.section}>Month {month?.month_number||'—'} payout</Text><Text style={{fontSize:24,fontWeight:'800'}}>{money(payout)}</Text><Text style={s.muted}>Scheduled date: {String(scheduledDate||'—').slice(0,10)}</Text><Badge tone={status==='COMPLETED'?'green':drawOpened?'purple':'orange'}>{status}</Badge></Card>
  {!drawOpened&&operate&&<Card><Text style={s.section}>Open Fixed Draw / Interest Window</Text>{!scheduleReady&&<Text style={s.danger}>Opening is available on the scheduled date. Test bypass is currently disabled.</Text>}<Button title={bypass?'Open Fixed Draw (TEST)':'Open Fixed Draw'} onPress={start} disabled={busy||!scheduleReady}/></Card>}
  {drawOpened&&interestOpen&&isMember(user)&&<Card><Text style={s.section}>Your interest</Text><Text style={s.muted}>Choose whether you want to participate in this month's draw.</Text><View style={s.row}><View style={{flex:1}}><Button title="Interested" onPress={()=>interest(true)} disabled={busy}/></View><View style={{flex:1}}><Button title="Not interested" secondary onPress={()=>interest(false)} disabled={busy}/></View></View></Card>}
  {drawOpened&&isMember(user)&&!interestOpen&&<Card><Text style={s.muted}>The interest window is not currently open.</Text></Card>}
  {drawOpened&&Array.isArray(state?.participants)&&state.participants.length>0&&<Card><Text style={s.section}>Eligible members</Text>{state.participants.map((p:any)=><Card key={String(p.id)}><Text style={{fontWeight:'800'}}>{String(p.participant_sequence??p.participantSequence??'')} · {String(p.member_name??p.memberName??'Member')}</Text><Text style={s.muted}>{String(p.mobile||p.member_mobile||'')}</Text><Badge tone={String(p.interest_status||p.interestStatus)==='INTERESTED'?'green':'orange'}>{String(p.interest_status||p.interestStatus||'NO_RESPONSE')}</Badge></Card>)}</Card>}
  {operate&&drawOpened&&status!=='COMPLETED'&&<Button title={bypass?'Run Draw Now (TEST)':'Run Draw'} onPress={run} disabled={busy}/>}{state?.winner&&<Card><Text style={s.section}>Winner selected</Text><Text style={{fontSize:20,fontWeight:'800'}}>{String(state.winner.member_name||state.winner.memberName||`Member ${state.winner.participant_sequence??''}`)}</Text><Text style={s.muted}>Payout: {money(payout)}</Text></Card>}
 </ScrollView></Screen>}
