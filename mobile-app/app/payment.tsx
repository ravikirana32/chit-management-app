import React,{useCallback,useEffect,useState}from'react';import{Alert,ScrollView,Text,View}from'react-native';import{router,useLocalSearchParams}from'expo-router';import{paymentsApi,chitsApi,agentApi}from'@/src/api/all';import{Badge,Button,Card,Input,Loading,Screen,s}from'@/src/components/UI';import{date,errMsg,idempotency,money}from'@/src/lib/format';import{useAuth}from'@/src/state/Auth';import{isAdmin,isCreator,isAgent,isMember,canOperate}from'@/src/state/roles';export default function Payment(){const{chitId,monthId}=useLocalSearchParams<{chitId:string;monthId:string}>();const{user}=useAuth();const[data,setData]=useState<any>();const[pays,setPays]=useState<any[]>([]);const[chit,setChit]=useState<any>();const[access,setAccess]=useState<any>();const[amount,setAmount]=useState('');const[method,setMethod]=useState('UPI');const[reference,setReference]=useState('');const[busy,setBusy]=useState(false);const load=useCallback(async()=>{try{const[o,p,c]=await Promise.all([paymentsApi.obligations(String(chitId),String(monthId)),paymentsApi.list(String(chitId),String(monthId)),chitsApi.get(String(chitId))]);setData(o.data?.data??o.data);setPays(p.data?.data?.payments??p.data?.data??[]);const cd=c.data?.data??c.data;setChit(cd);if(isAgent(user)&&!isCreator(user,cd))try{const a=await agentApi.chit(String(chitId));setAccess(a.data?.data??a.data)}catch{}}catch(e){Alert.alert('Unable to load',errMsg(e))}},[chitId,monthId,user]);useEffect(()=>{if(chitId&&monthId)load()},[load]);if(!data)return <Screen title="Payments"><Loading/></Screen>;const mine=data.obligations?.find((o:any)=>o.chitParticipantId===user?.participantId);const canVerify=canOperate(user,chit,'can_verify_payments',access);const submit=async()=>{if(!mine)return Alert.alert('No obligation','Your participant/obligation is not available');const n=Number(amount||mine.outstandingAmount);if(n<=0||n>Number(mine.outstandingAmount))return Alert.alert('Invalid amount');if(!reference)return Alert.alert('Reference required','Use UPI-123 or CASH-001');setBusy(true);try{await paymentsApi.submit(String(chitId),String(mine.chitParticipantId),{amount:n.toFixed(2),paymentMethod:method,transactionReference:reference,paymentDate:new Date().toISOString(),obligationId:mine.id,notes:'Submitted from mobile',idempotencyKey:idempotency()});Alert.alert('Submitted','Payment is pending verification');setAmount('');setReference('');load()}catch(e){Alert.alert('Payment failed',errMsg(e))}finally{setBusy(false)}};const verify=async(p:any)=>{setBusy(true);try{await paymentsApi.verify(p.id,{status:'VERIFIED'});load()}catch(e){Alert.alert('Verification failed',errMsg(e))}finally{setBusy(false)}};return <Screen title={`Month ${data.monthNumber}`} subtitle={`${date(data.scheduledDate)} · ${money(data.scheduledAmount)}`} back={()=>router.back()}><ScrollView>{mine&&<Card><Text style={s.section}>My obligation</Text><Text>Due {money(mine.dueAmount)} · Paid {money(mine.paidAmount)} · Outstanding {money(mine.outstandingAmount)}</Text><Badge tone={mine.status==='VERIFIED'?'green':'orange'}>{mine.status}</Badge>{isMember(user)&&Number(mine.outstandingAmount)>0&&<><Input label="Amount" value={amount} onChangeText={setAmount} placeholder={String(mine.outstandingAmount)} keyboardType="decimal-pad"/><Input label="Transaction / cash reference" value={reference} onChangeText={setReference} placeholder="UPI-123 or CASH-001"/><View style={s.row}>{['UPI','CASH','BANK_TRANSFER'].map(x=><Button key={x} title={x} secondary={method!==x} onPress={()=>setMethod(x)}/>)}</View><Button title="Submit Contribution" onPress={submit} disabled={busy}/></>}</Card>}{canVerify&&<Card><Text style={s.section}>Verify member payments</Text>{pays.map(p=><Card key={p.id}><Text style={{fontWeight:'800'}}>Member {p.participantSequence||p.participant_sequence||''}</Text><Text>{money(p.amount)} · {p.paymentMethod||p.payment_method} · {p.transactionReference||p.transaction_reference||'—'}</Text><Badge tone={p.status==='VERIFIED'?'green':'orange'}>{p.status}</Badge>{p.status!=='VERIFIED'&&<Button title="Verify" onPress={()=>verify(p)}/>}</Card>)}</Card>}<Button title="Refresh" secondary onPress={load}/></ScrollView></Screen>}
