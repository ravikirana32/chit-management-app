import React from'react';import{Text}from'react-native';import{router}from'expo-router';import{Button,Card,Screen,s}from'@/src/components/UI';import{useAuth}from'@/src/state/Auth';export default function Profile(){const{user,logout}=useAuth();return <Screen title="Profile" back={()=>router.back()}><Card><Text style={s.section}>{user?.name||'User'}</Text><Text>Mobile: {user?.mobile||'—'}</Text><Text>Roles: {user?.roles?.join(', ')||'MEMBER'}</Text><Text>Participant ID: {user?.participantId||'—'}</Text></Card><Button title="Payment Profile" secondary onPress={()=>router.push('/payment-profile')}/><Button title="Notification Preferences" secondary onPress={()=>router.push('/notification-preferences')}/><Button title="Notifications" secondary onPress={()=>router.push('/notifications')}/><Button title="Logout" danger onPress={async()=>{await logout();router.replace('/login')}}/></Screen>}
