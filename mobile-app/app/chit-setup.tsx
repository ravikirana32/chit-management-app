import React,{useEffect,useState}from'react';import{Alert,ScrollView,Text}from'react-native';import{router,useLocalSearchParams}from'expo-router';import{chitsApi,usersApi}from'@/src/api/all';import{Button,Card,Input,Loading,Screen,Select,s}from'@/src/components/UI';import{errMsg}from'@/src/lib/format';import{useAuth}from'@/src/state/Auth';import{isAdmin,isAgent}from'@/src/state/roles';
export default function ChitSetup(){const{chitId}=useLocalSearchParams<{chitId:string}>();const[chit,setChit]=useState<any>();const[months,setMonths]=useState<any[]>([]);const[agents,setAgents]=useState<any[]>([]);const{user}=useAuth();const[busy,setBusy]=useState(false);useEffect(()=>{if(isAdmin(user))usersApi.adminAgents().then(r=>setAgents(r.data?.data??[])).catch(()=>{});if(chitId)chitsApi.get(String(chitId)).then(r=>{const d=r.data?.data??r.data;setChit(d);setMonths(d.months||[])})},[chitId,user]);if(!chit)return <Screen title="Schedule"><Loading/></Screen>;const update=(i:number,k:string,v:any)=>setMonths(x=>x.map((m,j)=>j===i?{...m,[k]:v}:m));const save=async()=>{setBusy(true);try{await chitsApi.saveSchedule(String(chitId),{totalChitAmount:String(chit.total_chit_amount),months:months.map(m=>({monthNumber:Number(m.month_number),scheduledDate:m.scheduled_date,scheduledAmount:String(m.scheduled_amount),winnerPayoutAmount:String(m.winner_payout_amount),monthType:m.month_type,agentId:m.agent_id||undefined}))});Alert.alert('Saved','Schedule updated')}catch(e){Alert.alert('Save failed',errMsg(e))}finally{setBusy(false)}};const publish=async()=>{setBusy(true);try{await chitsApi.publish(String(chitId));Alert.alert('Published','Invite/confirm members now');router.replace({pathname:'/chit-detail',params:{chitId:String(chitId)}})}catch(e){Alert.alert('Publish failed',errMsg(e))}finally{setBusy(false)}};return <Screen title="Monthly Schedule" subtitle={chit.name} back={()=>router.back()}><ScrollView>{months.map((m,i)=><Card key={m.id}><Text style={s.section}>Month {m.month_number}</Text><Select label="Type" value={m.month_type} options={[{label:'ACTION',value:'ACTION'},{label:'AGENT CHIT',value:'AGENT_CHIT'}]} onChange={v=>{update(i,'month_type',v);if(v==='AGENT_CHIT'&&!m.agent_id&&isAgent(user))update(i,'agent_id',user.id)}}/><Input label="Scheduled date" value={String(m.scheduled_date)} onChangeText={v=>update(i,'scheduled_date',v)}/><Input label="Contribution/member" value={String(m.scheduled_amount)} onChangeText={v=>update(i,'scheduled_amount',v)} keyboardType="decimal-pad"/><Input label="Payout" value={String(m.winner_payout_amount)} onChangeText={v=>update(i,'winner_payout_amount',v)} keyboardType="decimal-pad"/>{m.month_type==='AGENT_CHIT'&&(isAdmin(user)?<Select label="Agent" value={m.agent_id||''} options={agents.filter((a:any)=>a.status==='ACTIVE').map((a:any)=>({label:`${a.name} · ${a.mobile}`,value:a.id}))} onChange={v=>update(i,'agent_id',v)} placeholder="Select active agent"/>:<Text style={s.muted}>Agent: {m.agent_id||user?.id||'Configured agent'}</Text>)}</Card>) }<Button title="Save Schedule" onPress={save} disabled={busy}/><Button title="Publish Chit" onPress={publish} disabled={busy}/></ScrollView></Screen>}
