import React,{useState}from'react';import{Alert,KeyboardAvoidingView,Platform,Text,View}from'react-native';import{router}from'expo-router';import{Button,Input,s}from'@/src/components/UI';import{authApi}from'@/src/api/all';import{useAuth}from'@/src/state/Auth';import{errMsg}from'@/src/lib/format';
export default function Login(){const{login}=useAuth();const[mobile,setMobile]=useState('');const[otp,setOtp]=useState('');const[sent,setSent]=useState(false);const[busy,setBusy]=useState(false);async function request(){if(mobile.replace(/\D/g,'').length<10)return Alert.alert('Invalid mobile','Enter a valid mobile number');setBusy(true);try{await authApi.requestOtp(mobile.trim());setSent(true);Alert.alert('OTP requested','Enter the OTP sent to your mobile.')}catch(e){Alert.alert('Unable to request OTP',errMsg(e))}finally{setBusy(false)}}async function verify(){if(otp.length<4)return Alert.alert('OTP required','Enter the OTP');setBusy(true);try{await login(mobile.trim(),otp.trim());router.replace('/')}catch(e){Alert.alert('Login failed',errMsg(e))}finally{setBusy(false)}}return <KeyboardAvoidingView behavior={Platform.OS==='ios'?'padding':undefined} style={s.screen}><View style={{marginTop:80}}><Text style={s.title}>Chit Management</Text><Text style={[s.subtitle,{fontSize:16,marginBottom:28}]}>Secure monthly chit management</Text><Input label="Mobile number" value={mobile} onChangeText={setMobile} placeholder="+919999999999" keyboardType="phone-pad" autoCapitalize="none"/><Button title="Request OTP" onPress={request} disabled={busy}/>{sent&&<><Input label="OTP" value={otp} onChangeText={setOtp} placeholder="1234" keyboardType="number-pad" maxLength={8}/><Button title="Verify & Login" onPress={verify} disabled={busy}/></>}</View></KeyboardAvoidingView>}
