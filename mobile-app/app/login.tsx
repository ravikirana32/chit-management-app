import React,{useState}from'react';import{Alert,KeyboardAvoidingView,Platform,Text,View}from'react-native';import{router}from'expo-router';import{Button,Input,s}from'@/src/components/UI';import{useAuth}from'@/src/state/Auth';import{authApi}from'@/src/api/all';import{errMsg}from'@/src/lib/format';
export default function Login(){const{login}=useAuth();const[mobile,setMobile]=useState('');const[otp,setOtp]=useState('');const[devOtp,setDevOtp]=useState('');const[sent,setSent]=useState(false);const[busy,setBusy]=useState(false);const dev=process.env.EXPO_PUBLIC_DEV_LOGIN==='true';const request=async()=>{if(mobile.replace(/\D/g,'').length<10)return Alert.alert('Invalid mobile','Enter a valid mobile number');setBusy(true);try{const r=await authApi.requestOtp(mobile.trim());const d=r.data?.data??r.data;setSent(true);if(d?.devOtp)setDevOtp(String(d.devOtp));Alert.alert('OTP requested',d?.devOtp?`Development OTP: ${d.devOtp}`:'Enter the OTP sent to your mobile.')}catch(e){Alert.alert('Unable to request OTP',errMsg(e))}finally{setBusy(false)}};const verify=async(code:string)=>{if(!code.trim())return Alert.alert('OTP required','Enter the OTP');setBusy(true);try{await login(mobile.trim(),code.trim());router.replace('/dashboard')}catch(e){Alert.alert('Login failed',errMsg(e))}finally{setBusy(false)}};return <KeyboardAvoidingView behavior={Platform.OS==='ios'?'padding':undefined} style={s.screen}><View style={{marginTop:60}}><Text style={s.title}>Chit Management</Text><Text style={[s.subtitle,{fontSize:16,marginBottom:28}]}>Role-aware monthly chit management</Text>{dev&&<View style={{padding:14,borderWidth:1,borderRadius:12,marginBottom:18}}><Text style={{fontWeight:'800'}}>DEVELOPMENT MODE</Text><Text style={s.muted}>OTP is still issued and verified through the backend challenge flow.</Text></View>}<Input label="Mobile number" value={mobile} onChangeText={setMobile} placeholder="+919999999999" keyboardType="phone-pad"/><Button title={sent?'Resend OTP':'Request OTP'} onPress={request} disabled={busy}/>{sent&&<><Input label="OTP" value={otp} onChangeText={setOtp} placeholder={devOtp||'123456'} keyboardType="number-pad"/><Button title="Verify & Login" onPress={()=>verify(otp||devOtp)} disabled={busy}/></>}</View></KeyboardAvoidingView>}
