import React,{useEffect,useState}from'react';import{Alert,ScrollView,Text}from'react-native';import{router}from'expo-router';import{usersApi}from'@/src/api/all';import{Button,Card,Input,Loading,Screen,s,Badge,Select}from'@/src/components/UI';import{useAuth}from'@/src/state/Auth';import{isAdmin}from'@/src/state/roles';import{errMsg}from'@/src/lib/format';
export default function AdminAgents(){const{user}=useAuth();const[data,setData]=useState<any[]>([]);const[users,setUsers]=useState<any[]>([]);const[name,setName]=useState('');const[mobile,setMobile]=useState('');const[userId,setUserId]=useState('');const[busy,setBusy]=useState(false);const load=async()=>{try{const[a,u]=await Promise.all([usersApi.adminAgents(),usersApi.adminUsers()]);setData(Array.isArray(a.data?.data)?a.data.data:[]);setUsers((u.data?.data??[]).filter((x:any)=>(x.roles||[]).includes('AGENT')&&String(x.status).toUpperCase()!=='DELETED'))}catch(e){Alert.alert('Unable to load',errMsg(e))}};useEffect(()=>{if(isAdmin(user))load()},[user]);if(!isAdmin(user))return <Screen title="Access denied"/>;const create=async()=>{if(!name.trim()||!mobile.trim()||!userId)return Alert.alert('Name, mobile and user are required');setBusy(true);try{await usersApi.createAgent({name:name.trim(),mobile:mobile.trim(),userId});setName('');setMobile('');setUserId('');Alert.alert('Agent created');load()}catch(e){Alert.alert('Create failed',errMsg(e))}finally{setBusy(false)}};const remove=(id:string,name:string)=>Alert.alert('Deactivate agent?',`${name||'This agent'} will be deactivated and removed from active chit assignments.`,[{text:'Cancel',style:'cancel'},{text:'Deactivate',style:'destructive',onPress:async()=>{try{await usersApi.deleteAgent(id);load()}catch(e){Alert.alert('Deactivate failed',errMsg(e))}}}]);return <Screen title="Agents" subtitle="Manage active and inactive agent profiles" back={()=>router.back()}><ScrollView><Card><Text style={s.section}>Create agent profile</Text><Input label="Agent name" value={name} onChangeText={setName}/><Input label="Mobile" value={mobile} onChangeText={setMobile} keyboardType="phone-pad"/><Select label="Existing AGENT user" value={userId} options={users.map(x=>({label:`${x.name} · ${x.mobile}`,value:String(x.id)}))} onChange={setUserId} placeholder="Select user"/><Button title="Create Agent Profile" onPress={create} disabled={busy}/></Card>{data.map(a=><Card key={a.id}><Text style={{fontWeight:'800'}}>{String(a.name||'Unnamed agent')}</Text><Text>{String(a.mobile||'—')} · {String(a.email||'—')}</Text><Badge tone={String(a.status).toUpperCase()==='ACTIVE'?'green':'red'}>{String(a.status||'UNKNOWN')}</Badge><Text style={s.muted}>Agent ID: {String(a.id)}</Text><Button title="Edit Agent" secondary onPress={()=>router.push({pathname:'/edit-agent',params:{agentId:String(a.id)}})}/>{String(a.status).toUpperCase()==='ACTIVE'&&<Button title="Deactivate Agent" danger onPress={()=>remove(String(a.id),String(a.name||''))}/>}</Card>)}</ScrollView></Screen>}
